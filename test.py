print("Welcome to rectangle area finder:")

units = input("What units are we using? ")
length = float(input("What is the length? "))
width = float(input("What is the width? "))

print("The area of your rectangle is " + str(length*width) + " " + units + " squared")

